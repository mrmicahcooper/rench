#!/usr/bin/env ruby

require 'pathname'

$:.unshift Pathname.new(__FILE__).join('../../lib').expand_path.to_s
require 'wrench'

if ARGV.size < 2
  p "Please enter a Github username and file location"
  exit 1
end

Wrench::CLI.new(*ARGV).download_file
