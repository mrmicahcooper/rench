#!/usr/bin/env ruby

require 'pathname'

$:.unshift Pathname.new(__FILE__).join('../../lib').expand_path.to_s
require 'rench'

if ARGV.size < 2
  $stdout.puts "Please enter a Github username and filename "
else
  Rench::CLI.new(*ARGV).download_file
end

